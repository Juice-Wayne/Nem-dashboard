NODE_TLS_REJECT_UNAUTHORIZED=0
NEOPOINT_API_KEY=ALE456879
# VPN IP whitelist — comma-separated public egress IPs.
# On Vercel: blocks requests not from VPN egress IP.
# Locally: middleware allows localhost, but client-side VPN check (via /api/vpn-status) blocks if VPN adapter is missing.
ALLOWED_IPS=20.53.131.91
